/**
 * Client configuration constants
 * This file contains network endpoints and other client-side configuration values
 */

/**
 * Sui blockchain RPC endpoint URL
 * Used for all blockchain interactions with the Sui network
 * Current network: devnet
 */
export const SUI_RPC_URL = 'https://fullnode.devnet.sui.io';
